//Minimum Maximum

var min =Math.min(7, 5, -12, 6, 32, 18, 14, -2);
var max =Math.max(-3, 9, 21, 36, 27, 54, 17, 35);

var result = max + min;

//Aléatoire 1
var floatBateau = 10.4;
var couleBateau = Math.floor(floatBateau);
var voleBateau = Math.ceil(floatBateau);

//Aléatoire 2

var cinquanteCent = Math.random()*(100-50)+50;
var zeroUn = Math.round(Math.random());

//Aléatoire 3 
var persoAnime = ["Goro", "Jhonny Cago", "Kano", "Liu Kano", "Raiden", "Reptil","Scorpion", "Shang Tsun","Sonya", "sub-zero"];
var A = Math.round(Math.random()*10);
console.log(persoAnime[A]);
